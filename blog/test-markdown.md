---
title: Testing Markdown Rendering - All Elements
date: 2026-05-30
---

# Main Heading Test

This is a paragraph with some **bold text** and *italic text* to test basic formatting. The article should render with the Merriweather font and light white color (#d4d4d4).

## Subheading Test

Here's another paragraph to test how multiple paragraphs look together. This one has some more content to see line breaks and spacing.

### Smaller Heading

This is a smaller heading to test h3 rendering.

## Code Block Test

Here's a simple JavaScript code block:

```javascript
function helloWorld() {
  console.log("Hello, World!");
  return true;
}
```

And here's a Python example:

```python
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)

result = fibonacci(10)
print(f"Fibonacci of 10: {result}")
```

## Lists Test

Here are some bullet points:

- First item in the list
- Second item with more text
- Third item
- Fourth item

## HTML/Web Example

Here's an HTML code block:

```html
<!DOCTYPE html>
<html>
  <head>
    <title>Test Page</title>
  </head>
  <body>
    <h1>Hello World</h1>
    <p>This is a test paragraph.</p>
  </body>
</html>
```

## PHP Code Block

```php
<?php
$name = "Kymu";
$year = 2026;

echo "Welcome, $name!";
echo "Year: $year";
?>
```

## SQL Example

```sql
SELECT * FROM users 
WHERE created_at > '2026-01-01' 
AND status = 'active' 
ORDER BY id DESC;
```

## Conclusion

This test article covers:
- Multiple headings (h1, h2, h3)
- Paragraphs with multiple lines
- Unordered lists
- Multiple code blocks in different languages
- Font and color rendering

Everything should display with the Merriweather serif font, light white text, and proper spacing!
